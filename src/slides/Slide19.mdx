import Layout from '../components/Layout'
import AppearText from '../components/AppearText'

> # Modern web development architecture based on client-side JavaScript, reusable APIs, and prebuilt Markup.
>
> _by [jamstack.org](https://jamstack.org)_

<AppearText>
  The JAMstack is a name to a set of widely used architectural practices.
  It gives us one word to communicate a large range of architectural
  decisions.
</AppearText>

export default ({ children }) => <Layout title="React Training">{ children }</Layout>
