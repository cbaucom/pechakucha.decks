import Layout from '../components/Layout'

  <h1>
    Monolithic apps need to generate and deliver HTML every
    time a new visitor arrives on the site
  </h1>


export default ({ children }) => <Layout title="React Training">{ children }</Layout>
